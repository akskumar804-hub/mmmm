# Frontend API base URL
# IMPORTANT: It should include /api
VITE_API_URL=https://mtmmm-2.onrender.com/api


# Proctoring (optional)
# 1 = show webcam proctoring option (with explicit consent), 0 = hide it
VITE_PROCTOR_WEBCAM=0
# Max warnings before auto-submit
VITE_PROCTOR_MAX_WARNINGS=3
# Webcam snapshot interval (seconds) when enabled
VITE_PROCTOR_SNAPSHOT_INTERVAL_SECONDS=60

# Require screen share before starting exam (recommended for high-stakes)
VITE_PROCTOR_REQUIRE_SCREENSHARE=0
