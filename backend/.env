# Backend configuration
PORT=4000

# Frontend URL (for CORS + password reset link)
CORS_ORIGIN=http://localhost:5173
APP_URL=http://localhost:5173

# Database (REQUIRED)
# For local MySQL: ensure MySQL server is running on localhost:3306
# For remote MySQL: use the full hostname provided by your hosting
DATABASE_URL=mysql://root:TgHtJhegEuocQuPLrLObTUwfdrKahKFL@trolley.proxy.rlwy.net:55140/railway

# Upload directory
# For Render Persistent Disk, mount a disk (e.g. /data) and set:
# UPLOAD_DIR=/data/uploads
UPLOAD_DIR=./uploads

# Auth
JWT_SECRET=change_me_to_a_long_random_string

# Hard-coded admin login (seeded on backend startup)
ADMIN_EMAIL=lms_admin@montavales.com
ADMIN_PASSWORD=lms@admin*83
ADMIN_NAME=Admin
ADMIN_LOGIN_CODE=999999

# Exam rules
RESULT_RELEASE_DAYS=3
# Retake cooldown starts AFTER result release
RETAKE_GAP_DAYS=3

# SMTP (optional) - if not set, emails are logged to console
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=lms.noreply995@gmail.com
SMTP_PASS=qaztyhqvtiaszvov
EMAIL_FROM=LMS <lms.noreply995@gmail.com>

# Razorpay (optional) - for online payments
# If not set, students can still upload receipts and admin can confirm manually.
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=


# Proctoring (optional)
# Max upload size per webcam snapshot (bytes). Default 2MB.
PROCTOR_SNAPSHOT_MAX_BYTES=2000000


# Proctoring behavior
# If 1, exam submission requires an active proctor session (recommended).
PROCTOR_REQUIRED=1

# Max warnings before the student is auto-submitted (frontend also enforces).
PROCTOR_MAX_WARNINGS=3

# Randomization: pick only N questions per attempt (0 = all questions)
EXAM_QUESTIONS_PER_ATTEMPT=0
